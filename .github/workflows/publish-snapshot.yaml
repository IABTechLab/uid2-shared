name: Publish Snapshot Package
on:
  workflow_dispatch:

jobs:
  build-publish-package:
    uses: IABTechLab/uid2-shared-actions/.github/workflows/test-commit.yaml@llp-uid2-2236-auto-pr-action
    secrets: inherit
