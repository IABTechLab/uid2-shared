package com.uid2.shared.audit;

/**
 * An enum of all table entities that uid2-admin handles.
 */

public enum Type {
    SITE,
    CLIENT,
    KEYACL,
    KEY,
    SALT,
    OPERATOR,
    ENCLAVE,
    PARTNER,
    ADMIN
}
